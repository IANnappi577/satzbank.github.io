let meisterWortListe;


function verbenHinzufügen(event) {
    event.preventDefault();

    // Wortstring abrufen
    let wortStr = document.getElementById("verbWortliste");

    // let box = document.getElementById("verben");

    // update html by inserting the word in the right order and writing all back to string
    // e.g. sort list, then convert to string, then store back

    // use localStorage.setItem("item", itemText); to store stuff locally

    wortStr.value = "";
}

function nomenHinzufügen() {
}

function adjektiveHinzufügen() {
}